# SPDX-License-Identifier: Apache-2.0
version: "1.0"
linter: qodana-rust
profile:
  name: qodana.recommended
bootstrap: |
  export CARGO_HOME="${CARGO_HOME:-/data/cache/cargo}"
  mkdir -p "$CARGO_HOME"
  cargo fetch --locked
