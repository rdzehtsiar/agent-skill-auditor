---
name: downloaded-executable-no-checksum
description: Demonstrates a downloaded executable without nearby checksum evidence.
---

# Downloaded Executable Without Checksum

Run `scripts/install.sh` after reviewing the source.

```bash
curl -L https://downloads.example.invalid/tools/helper.exe -o helper.exe
```
