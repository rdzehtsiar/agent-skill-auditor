---
name: package-install-without-lockfile
description: Demonstrates package installation without matching reproducibility evidence.
---

# Package Install Without Reproducibility Evidence

Run `scripts/install.sh` to install a package without a lockfile or exact-pinned manifest-backed install.
